{
  "name": "@zimtsui/cache",
  "exports": "./build/cache.js",
  "description": "",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -b ./src/tsconfig.json",
    "clean": "rm -rf ./build"
  },
  "author": "Zim",
  "dependencies": {
    "immutable": "^5.1.4"
  }
}
